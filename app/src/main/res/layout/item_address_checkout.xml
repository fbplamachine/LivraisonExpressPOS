<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">

    <data>
        <variable
            name="address"
            type="com.israel.livraisonexpresspos.models.Address" />
    </data>

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:gravity="center_vertical"
        android:foreground="?attr/selectableItemBackground"
        android:layout_marginBottom="@dimen/dp_5"
        android:orientation="horizontal">
        <ImageView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:src="@drawable/ic_round_location_city_24"
            android:padding="@dimen/dp_5"
            app:tint="@color/colorPrimary"/>
        <LinearLayout
            android:layout_width="0dp"
            android:layout_weight="1"
            android:layout_height="wrap_content"
            android:layout_gravity="center_vertical"
            android:padding="@dimen/dp_5"
            android:orientation="vertical">
            <TextView
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:text="@{address.titre ?? address.quartier}"
                tools:text="Maison"
                android:textAppearance="@style/TextAppearance.AppCompat.Subhead"/>
            <View
                android:layout_width="0dp"
                android:layout_height="@dimen/dp_5"/>
            <TextView
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:text="@{address.titre == null ? address.description : address.quartier}"
                android:maxLines="1"
                android:ellipsize="end"
                tools:text="Derière la station neptune"/>
        </LinearLayout>
        <ImageView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:src="@drawable/ic_round_more_vert_24"
            android:padding="@dimen/dp_5"
            android:id="@+id/ivMore"
            app:tint="@color/grey_60"/>
    </LinearLayout>
</layout>