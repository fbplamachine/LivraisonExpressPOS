<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">

    <data>
        <import type="android.view.View"/>
        <variable
            name="manager"
            type="com.israel.livraisonexpresspos.models.inventory_management_models.User" />
        <variable
            name="displayDivider"
            type="Boolean" />
    </data>

    <androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
        android:layout_width="match_parent"
        android:layout_height="wrap_content">

        <View
            android:id="@+id/divider"
            android:layout_width="match_parent"
            android:layout_height="@dimen/dp_1"
            android:background="@color/grey_20"
            android:visibility="@{displayDivider? View.VISIBLE : View.GONE}"
            app:layout_constraintTop_toTopOf="parent"/>

        <ImageView
            android:id="@+id/icon"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:src="@drawable/ic_person_black_24"
            android:layout_marginTop="@dimen/dp_5"
            android:layout_marginStart="@dimen/dp_10"
            app:layout_constraintTop_toBottomOf="@+id/divider"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintEnd_toStartOf="@+id/tv_manager"/>

        <TextView
            android:id="@+id/tv_manager"
            android:layout_width="@dimen/dp_0"
            android:layout_height="wrap_content"
            tools:text="Nom du gestionnaire"
            android:layout_marginTop="@dimen/dp_5"
            android:text="@{manager.fullname}"
            android:ellipsize="end"
            android:maxLines="1"
            android:padding="@dimen/dp_10"
            app:layout_constraintStart_toEndOf="@+id/icon"
            app:layout_constraintTop_toBottomOf="@+id/divider"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintEnd_toStartOf="@+id/btn_menu"/>

        <ImageButton
            android:id="@+id/btn_menu"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:src="@drawable/ic_round_more_vert_24"
            android:layout_marginTop="@dimen/dp_5"
            android:tint="@color/grey_90"
            android:layout_marginEnd="@dimen/dp_10"
            android:background="@color/transparent"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintStart_toEndOf="@id/tv_manager"
            app:layout_constraintTop_toBottomOf="@+id/divider"
            app:layout_constraintBottom_toBottomOf="@id/tv_manager"/>

    </androidx.constraintlayout.widget.ConstraintLayout>
</layout>